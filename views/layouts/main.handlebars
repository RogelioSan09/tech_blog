<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
</head>

<body>
    <header>
        <h1>Tech Blog</h1>
    </header>
    {{!-- Nav bar will display Home and Logout(if available) buttons--}}
    <nav>
        <a href="/">Home</a>
        {{!-- When user is logged in, the button to logout will be available --}}
        {{#if loggedIn}}
        <button id="logout" class="btn-no-style">Logout</button>
        {{else}}
        {{!-- if user is not logged in or log-in is unsuccessful, the user willl be redirected to the login page --}}
        <a href="/login">Login</a>
        {{/if}}
    </nav>
    <main>
        <aside>
        <h2>The Tech Blog</h2>
        </aside>
        <div class="user-blogs">
        {{!-- data received from middleware will be displayed in body --}}
        {{{body}}}
        </div>
    </main>
    {{!-- when user is logged in, the script for logout will be available for execution --}}
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>


</html>